#include "pch.h"
#include <iostream>
using namespace std;
/*
int main()
{
	setlocale(LC_ALL, "Rus");
	struct music{
		char Executor[30];//Исполнитель
		char Genre[30];//Жанр
		char Name[30];//Название
		short Circulation;//Тираж
	};

	short AvrgCirculation = 10000;

	short n;
	cout << "Введите количество исполнителей:\n";
	cin >> n;
	cin.ignore();
	 music song[10];

	 for (int i = 0; i < n; i++) {

		 cout << "Введите имя Исполнителя\n";
		 cin.getline(song[i].Executor, 30);
		 cout << "Введите жанр Песни\n";
		 cin.getline(song[i].Genre, 30);
		 cout << "Введите название песни\n";
		 cin >> (song[i].Name);
		 cout << "Введите тираж\n";
		 cin >> (song[i].Circulation);
		 cin.ignore();

	 }

	 for (int i = 0; i < n; i++) {
		 if (song[i].Circulation > AvrgCirculation)
		 {

			 cout << endl;
			 cout << "Имя Исполнителя\n";
			 cout << (song[i].Executor) << endl;
			 cout << "Жанр Песни\n";
			 cout << (song[i].Genre) << endl;
			 cout << "Название песни\n";
			 cout << (song[i].Name) << endl;
			 cout << "Тираж\n";
			 cout << (song[i].Circulation) << endl;
			 cin.ignore();

		 }

	 }
	 return 0;
*/